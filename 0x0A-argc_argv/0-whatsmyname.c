#include <stdio.h>
/**
 * main - program that prints its name and a new line
 * @argc: the argc parameter from main
 * @argv: the argv parameter from main
 * Return: 0 - success.
 */
int main(int argc __attribute__((unused)), char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
